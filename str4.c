#include<stdio.h>
#include<string.h> 
int main()
{
	char f[10],l[10];
		
	scanf("%s",&f);
	scanf("%s",&l);
	
	printf("size of first name = %d",strlen(f));
	printf("\n size of last name= %d",strlen(l));
}  